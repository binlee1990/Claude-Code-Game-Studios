Role: Senior technical program lead and repository state auditor for a Godot SRPG workspace.
Context: The repository is formally in Pre-Production and the current human-only gate remains visual sign-off plus subjective fun validation. The automatable P3 chain is already complete, but the workspace still contains non-human unfinished work in two forms: future designed systems that have not been converted into executable epics, and authority drift across plans, epics, stories, architecture, and traceability artifacts. Current repository evidence must outrank stale session logs or template placeholders.
Objective: Identify every remaining unfinished item that does not require a human to execute directly, convert that inventory into a standard solution package plus actionable subtasks, and save the resulting artifacts under `docs/active/`.
Success criteria: 1. Remaining non-human work is classified into authoritative categories with repository evidence. 2. Human-only work is explicitly excluded from the actionable scope. 3. The solution package compares options and recommends a smallest-sufficient path. 4. The subtask breakdown is actionable and separates authority-repair work from future backlog packaging. 5. The artifacts are saved in `docs/active/`.
Decomposition: 1. Read the current authority files, backlog indexes, design indexes, and architecture docs. 2. Distinguish true unfinished implementation from stale metadata, placeholders, and drift. 3. Inventory the remaining designed-but-unepiced systems and separate them from human-owned design or approval work. 4. Convert the evidence into an idea-to-solution standard package. 5. Produce a companion subtasks file in `docs/active/`. 6. Validate the task framing and read back the generated documents.
Methodology: SCQA to anchor the real problem against the current repository state, and MECE to split remaining work into authority repair, architecture and traceability sync, future vertical-slice backlog packaging, and deferred alpha backlog packaging.
Output: Markdown files in `docs/active/` containing a standard solution package and a subtask register for remaining non-human work.
Constraints: Do not fabricate unfinished work from historical logs when current authority files disagree. Do not count human visual sign-off, subjective fun validation, or human approval decisions as agent-executable tasks. Do not start code implementation or new feature execution. Keep the output grounded in local repository evidence only. Do not change existing code.
Non-assumptions: Do not assume a `Ready` or `Not yet created` label means missing implementation without checking code and tests. Do not assume every designed GDD should immediately become an implementation epic. Do not assume proposed ADRs can be auto-accepted without a human decision. Do not assume audio, art, or narrative content production belongs in the same execution lane as repository authority repair.
Verification: Validate this task file with `python C:\\Users\\888\\.codex\\skills\\reframe-and-execute\\scripts\\validate-task.py <task-file> --strict`, verify the generated docs exist under `docs/active/`, and read back the saved files to confirm that the solution, remaining-item inventory, and subtasks are present and internally consistent.
Confidence: high because the task is document-focused and the repository already exposes authoritative sources for stage, backlog, design inventory, and architecture drift.
Reproducibility: Repo root `D:\\work\\Games\\SRPG`; solution artifacts in `docs/active/`; design inputs from `design/gdd/`, production backlog inputs from `production/epics/`, and architecture inputs from `docs/architecture/`.
Baseline: Before execution, `docs/active/` only contains `srpg-current-overall-plan.md`; the repo shows 12 production epics complete, 23 designed systems total, empty TR registry, and multiple stale `Ready` / `Not yet created` placeholders in epic and story documents.
