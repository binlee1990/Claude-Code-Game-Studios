Role: Senior Godot gameplay engineer implementing the next ordered post-slice epic in the SRPG roadmap.
Context: The current project plan has completed the vertical-slice stabilization work and defines the next ordered epic sequence as `skill-system -> equipment-system -> character-management`. The user asked to continue judging the next execution items and execute them all. Given the existing ordering and the user's approval of the current battle slice, the next concrete execution unit is the full `skill-system` epic. Existing dependencies already present in the codebase include Unit, ClassComponent, ActionSystem, SaveData, and GameEvents.
Objective: Implement the full Skill System epic (stories 001-007) with code, tests, save/load support, and production-doc synchronization.
Success criteria: 1. A complete skill data model exists. 2. Proficiency/leveling, rank ceilings, traits, damage calculation, class-skill unlock/freeze, and save/load round-trips are implemented. 3. Unit and save integration are wired so skill state persists through existing serialization paths. 4. The full Godot test suite passes after the new tests are added. 5. Production epic/story status can be updated truthfully from repository evidence.
Decomposition: 1. Design a compact skill architecture around a skill data object plus per-unit skill component. 2. Implement the story 001-005 logic APIs and tests. 3. Integrate class-change behavior for story 006. 4. Extend Unit serialization for story 007 and add integration tests. 5. Run the full suite and sync production docs.
Methodology: SCQA to stay aligned with roadmap order and MECE to decompose the epic into data model, progression, constraints, modifiers, class integration, and persistence.
Output: New skill-system source files, unit/integration tests, updated manifests, and synced production status docs.
Constraints: Stay within the skill-system epic; do not begin equipment-system or character-management in the same pass. No new dependencies. Reuse existing Unit/Class/Save patterns where possible. Keep the public battle entry path stable.
Non-assumptions: Do not assume the epic should stop at story 001. Do not assume UI implementation is required beyond exposing data/state needed by the current battle/UI layers. Do not assume class-skill behavior can remain implicit without tests. Do not assume persistence is complete unless the round-trip tests prove it.
Verification: Add targeted `tests/unit/skill/*` and `tests/integration/skill/*`, register them in `tests/tests_manifest.txt`, and use `godot.windows.opt.tools.64.exe --headless res://tests/test_runner.tscn` plus `--check-only project.godot` as final gates.
Confidence: Medium-high because the epic is broad but strongly bounded, and the repository already has working patterns for formula testing, component serialization, and integration tests.
Reproducibility: Repo root `D:\work\Games\SRPG`; epic sources in `production/epics/skill-system/`; GDD in `design/gdd/skill-system.md`.
Baseline: Before edits, there is no `src/core/skill/` implementation, no `tests/unit/skill/` or `tests/integration/skill/` coverage, and the skill-system epic remains at `Ready` only.
Execution: Diagnose complete. This task is L3+ because it spans a full multi-story gameplay subsystem and its persistence/integration behavior.
