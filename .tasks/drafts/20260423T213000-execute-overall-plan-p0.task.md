Role: Senior Godot gameplay engineer executing the active repository plan from the current overall plan document.
Context: The user asked to execute the saved overall plan in `docs/active/srpg-current-overall-plan.md`. That plan explicitly prioritizes P0 automated baseline repair before any product-path integration. Current verified baseline: Godot project parses, but the full test run still has 5 compilation failures and 17 assertion failures. The highest-confidence root-cause buckets are attribute signal wiring, class experience/bonus logic, resource enum/test compilation issues, and a few incompatible test helpers.
Objective: Complete P0 from the overall plan by restoring the automated Godot test baseline to a trusted passing state, then reassess readiness for P1 product-path integration.
Success criteria: 1. Root causes for the known 5 compilation failures and 17 assertion failures are fixed in code or tests without widening scope. 2. `godot --headless res://tests/test_runner.tscn` completes without compilation failures and without assertion failures. 3. Any remaining issues are explicitly identified with evidence rather than hidden behind noise. 4. If P0 passes, the next executable step for P1 is clear.
Decomposition: 1. Reproduce and classify the current failing baseline. 2. Fix attribute signal/threshold propagation defects. 3. Fix class-system experience, level, and effective-bonus behavior. 4. Fix resource/type naming and test-script parse issues. 5. Re-run targeted tests, then the full suite, and only then move to P1 readiness evaluation.
Methodology: SCQA to keep execution aligned with the active plan and MECE to cluster failures into non-overlapping root-cause buckets instead of chasing individual tests in isolation.
Output: Code changes plus a concise execution report stating what was fixed, test evidence, and whether P0 is complete.
Constraints: Start with P0 only; do not jump into Camera/UI/productization work until the test baseline is trusted. No new dependencies. Keep diffs reviewable and local to failing systems/tests. Do not rewrite unrelated architecture.
Non-assumptions: Do not assume all failing tests are stale. Do not assume test failures are independent. Do not assume class-system expectations are wrong before checking the implementation and design intent. Do not assume prototype/product-path work can safely proceed with a noisy baseline.
Verification: Use targeted test runs while iterating and `godot.windows.opt.tools.64.exe --headless res://tests/test_runner.tscn` as the final gate. Use line-level code inspection to confirm each fix addresses the actual root cause. Re-check `--check-only project.godot` if parse instability appears.
Confidence: High that P0 can be substantially advanced in one execution pass because multiple failing tests collapse into a small number of shared defects.
Reproducibility: Windows PowerShell in `D:\work\Games\SRPG`; Godot executable resolved via `Get-Command godot.windows.opt.tools.64.exe`; plan source `docs/active/srpg-current-overall-plan.md`.
Baseline: Verified before edits: 5 test compilation failures, 17 assertion failures, and repeated runtime signal-argument errors originating from the attribute event chain.
Execution: Diagnose complete. This task is L3+ because it modifies multiple subsystems, requires executable verification, and serves as the gate for all downstream planned work.
