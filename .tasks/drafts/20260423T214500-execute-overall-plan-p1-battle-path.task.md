Role: Senior Godot gameplay engineer productizing the verified vertical-slice battle flow into the main repository path.
Context: P0 is complete: the full automated suite is now green. The next ordered item in `docs/active/srpg-current-overall-plan.md` is P1 product-path integration, specifically eliminating the gap where `main_menu -> battle` reaches an empty placeholder scene while the playable combat loop lives only under `prototypes/vertical-slice/`. The implementation should preserve a single battle-logic source of truth and add regression coverage for the formal entry scene.
Objective: Connect the playable vertical-slice battle controller to the formal `src/ui/combat/battle_arena.tscn` path so the main menu enters a functional battle slice instead of an empty placeholder.
Success criteria: 1. `src/ui/combat/battle_arena.tscn` instantiates into a playable battle flow. 2. Main menu / scene manager routing continues to use the stable `src/ui/combat/battle_arena.tscn` path. 3. Prototype and product paths share the same underlying battle controller logic rather than diverging copies. 4. Automated regression coverage exists for the formal battle entry scene, and the full suite remains green.
Decomposition: 1. Inspect current placeholder battle scene and prototype scene/controller. 2. Move or re-point the canonical battle controller into `src/ui/combat`. 3. Rewire the prototype scene to reuse the canonical controller instead of keeping a second logic copy. 4. Replace the empty formal battle scene with a scene that uses the verified controller. 5. Add/update tests and manifest entries. 6. Run the full suite again.
Methodology: SCQA to preserve the saved overall-plan ordering and MECE to separate routing concerns, scene/controller ownership, and regression evidence.
Output: Updated battle scenes/controller plus concise verification evidence that the formal entry path is now functional.
Constraints: Keep the stable `SceneManager` battle key and `battle_arena.tscn` path. Avoid duplicating battle logic across prototype and product scenes. Do not start Camera/UI feature expansion yet; only fix the product-path gap.
Non-assumptions: Do not assume renaming classes/files is necessary if a smaller canonicalization step works. Do not assume the prototype should remain the logic owner. Do not assume placeholder UI files must be fully deleted in the same step if they are simply unreferenced after canonicalization.
Verification: Use code inspection for scene/script wiring and run `godot.windows.opt.tools.64.exe --headless res://tests/test_runner.tscn` after adding a regression test for the formal battle entry scene.
Confidence: High because the playable controller already exists and the main work is canonicalizing scene ownership and regression coverage.
Reproducibility: Repo root `D:\work\Games\SRPG`; battle routing currently via `src/core/scene/scene_manager.gd` and `project.godot`.
Baseline: Before P1 edits, `project.godot` and `SceneManager` route into `src/ui/combat/battle_arena.tscn`, but that scene is an empty placeholder with a no-op HUD script; the playable battle slice exists only in `prototypes/vertical-slice/`.
Execution: Diagnose complete. This task is L3 because it touches formal scene ownership, shared controller placement, and automated regression evidence.
