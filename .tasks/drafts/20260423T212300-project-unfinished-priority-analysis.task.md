Role: Senior technical lead performing an evidence-backed brownfield project audit for a Godot SRPG repository.
Context: The repository contains design docs, production epics, core gameplay code, a vertical-slice prototype, and a Godot/GUT-based test suite. The user wants the remaining unfinished work across the whole project identified and prioritized by execution order. Existing `.omx` runtime files are dirty and must be ignored as non-product state. The audit must distinguish documented backlog, partially implemented systems, prototype-only work, broken baseline tests, and production/documentation drift.
Objective: Produce a verifiable project-wide unfinished-items inventory, grouped and sorted by execution priority, based on repository evidence rather than assumptions.
Success criteria: 1) Repo scope, current stage, implemented modules, and declared epics are inspected. 2) Unfinished items are backed by concrete code/doc/test evidence. 3) Priorities reflect dependency order, user value, and delivery risk. 4) The result separates confirmed gaps from assumptions or stale documentation.
Decomposition: 1. Read repository structure, stage docs, sprint docs, and epic index to define planned scope. 2. Inspect source, scenes, prototype, and tests to determine actual implementation coverage. 3. Run available verification entry points to confirm current baseline health. 4. Compare planned scope against actual implementation and cluster remaining work by root cause. 5. Sort clusters by execution priority and produce a concise, evidence-backed report.
Methodology: SCQA to frame the brownfield audit problem and MECE to partition unfinished work into baseline-health, vertical-slice integration, presentation/UI, feature backlog, and production-process gaps.
Output: Markdown audit with intent, evidence-backed unfinished items, priority order, rationale, and explicit verification notes.
Constraints: No code behavior changes. No new dependencies. Do not treat stale plans or session notes as ground truth without code/test corroboration. Ignore `.omx` runtime file churn. Keep conclusions compact and evidence-backed.
Non-assumptions: Do not assume an epic is complete because stories exist. Do not assume prototype functionality is wired into the shipping entry scene. Do not assume subsystem serialize/deserialize tests mean SaveManager-level persistence is complete. Do not assume outdated sprint TODO markers reflect current implementation state.
Verification: Cross-check production docs against `src/`, `tests/`, and runnable Godot checks. Use `project.godot` main scene, scene scripts, prototype scene, test runner output, and file-presence checks as evidence. Label items as verified gaps only when at least one concrete artifact supports them.
Confidence: High for code/test-backed gaps; medium for production-state drift because some status files are manually maintained.
Reproducibility: Windows PowerShell in `D:\work\Games\SRPG`; Godot 4.6.2 Steam executable discovered via `Get-Command godot.windows.opt.tools.64.exe`; test entry `--headless res://tests/test_runner.tscn`; project parse check `--headless --check-only project.godot`.
Baseline: Project is in Pre-Production / Vertical Slice build phase per `production/session-state/active.md`, with 5 completed core epics, 6 remaining Sprint-001 scene/UI stories, additional ready-but-unstarted feature/core epics, and a currently failing automated test baseline.
Execution: Diagnose complete. Complexity assessed as L3+ because the request spans the full repository, requires reconciliation of plan-vs-implementation-vs-verification, and has multiple independent evidence sources.
