Role: Senior Godot gameplay engineer continuing the validated SRPG roadmap after the vertical-slice stabilization pass.
Context: The repository is still formally in Pre-Production because the Pre-Production -> Production gate remains PARTIAL due to human-only visual sign-off and subjective fun validation. Automatable validation is already in place, `skill-system` is complete, `equipment-system` is the next ordered epic, `character-management` is the ordered follow-up epic, and the project also has document drift where at least one stage report is stale. Existing patterns for per-unit components, save/load round-trips, inventory resources, and event-driven cross-system integration must be reused instead of creating parallel infrastructure.
Objective: Execute the current automatable recommended backlog sequence end-to-end by synchronizing stale project status artifacts, implementing `equipment-system`, then implementing `character-management`, and finally revalidating the repository and status docs without overstating human-only gate progress.
Success criteria: 1. `equipment-system` stories 001-007 are represented by implementation and tests. 2. `character-management` stories 001-003 are represented by implementation and tests. 3. Save/load round-trips cover equipment state, roster state, deployment state, and their integration with existing unit data. 4. Production status documents reflect the real post-run state, including any remaining human-only blockers. 5. Automated verification passes.
Decomposition: 1. Normalize the execution target and identify reusable primitives in `Unit`, save/load, inventory, and existing formula modules. 2. Implement the equipment data model, loadout/set/decomposition/enhancement/final-attribute logic plus unit and integration tests. 3. Implement roster, deployment, departure/recall, and persistence logic plus unit and integration tests. 4. Sync production docs and stale stage-report artifacts from evidence. 5. Run repository verification and iterate on any issues discovered.
Methodology: SCQA to anchor the true execution target against current repository state and MECE to split work into doc-sync, equipment, character, and verification lanes without overlap.
Output: Source changes, test evidence, and synchronized production documents for the recommended backlog sequence.
Constraints: Keep the project formally in Pre-Production unless human-only gate evidence is actually produced. No new dependencies. No fabricated UI flows beyond what the existing logic/integration surfaces need. Reuse existing runtime/save patterns before adding abstractions. Keep changes reviewable and reversible.
Non-assumptions: Do not assume `Ready` epic docs imply implementation. Do not assume the stale phase report is authoritative. Do not mark the Production gate complete without genuine human visual/fun validation. Do not stop after `equipment-system` if `character-management` remains a directly recommended, automatable next step and the repository is still stable.
Verification: `godot.windows.opt.tools.64.exe --headless res://tests/test_runner.tscn` and `godot.windows.opt.tools.64.exe --headless --check-only project.godot`, plus targeted evidence that new equipment and character integration tests exist and pass.
Confidence: Medium-high because the existing repository already exposes strong patterns for unit components and save/load integration, but the scope spans two epics and shared serialization boundaries.
Reproducibility: Repo root `D:\work\Games\SRPG`; task artifacts in `.tasks/drafts/` and `.tasks/active/`; Godot project `project.godot`; test manifest `tests/tests_manifest.txt`.
Baseline: `equipment-system` and `character-management` are documented as Ready/Not started, `.tasks/active/` is empty, the gate is PARTIAL for human-only reasons, and `production/project-stage-report.md` is stale relative to current authoritative state.
Execution: Gate verdict = PASS. Target is explicit, environment is known, side effects are acceptable and local to the repository, and success signals are verifiable. Challenge refinements applied: the simpler path of doing only `equipment-system` was rejected because the user asked for comprehensive execution of the recommendation chain; the catastrophic-risk path of falsely upgrading the project phase was rejected by preserving human-only validation as an explicit unresolved blocker.
