Role: Senior Godot gameplay engineer executing the repository's active overall plan in dependency order.
Context: `docs/active/srpg-current-overall-plan.md` defines the current project strategy and makes P0 automated baseline restoration the first execution target. The repository currently contains working core systems, a prototype-only vertical slice, and a failing automated baseline. The user's request is to execute according to that document, so the safest valid interpretation is to start with P0 and carry it through verification before opening P1 work.
Objective: Restore the full automated Godot test baseline to a trusted passing state as the first completed phase of the saved overall plan.
Success criteria: 1. The known baseline failures are reduced to zero, or any irreducible residue is explicitly documented with evidence and blocked from silent degradation. 2. Root causes are fixed rather than papered over. 3. The final verification run demonstrates that the baseline is trustworthy enough to begin P1 product-path work.
Decomposition: 1. Reproduce failures and map them to shared causes. 2. Repair attribute signal propagation and threshold forwarding. 3. Repair class-system exp/level/bonus/save behavior. 4. Repair resource/test compilation issues and obsolete test helpers. 5. Run targeted regressions and then the full suite. 6. Decide whether P0 is complete and whether P1 may start.
Methodology: SCQA for execution framing and MECE for clustering failures into attribute, class, resource, and test-compatibility buckets.
Output: Working code, updated tests where required, and a concise execution summary with test evidence.
Constraints: Follow the existing overall plan ordering. Avoid speculative product/UI work until the baseline is green. No new dependencies. Preserve existing behavior unless the failing tests and implementation evidence prove the current behavior is wrong.
Non-assumptions: Do not assume test expectations are stale without code/design evidence. Do not assume all failures are independent. Do not assume parse errors come from engine install issues when script code clearly references unsupported APIs. Do not assume class exp caps imply level caps.
Verification: Iterate with code inspection and targeted checks; final gate is `godot.windows.opt.tools.64.exe --headless res://tests/test_runner.tscn`. If needed, use `--check-only project.godot` for parse sanity. Claims about completion must cite the verification result.
Confidence: Medium-high because the root-cause set is now narrow and concrete, but full-suite regressions can still reveal hidden coupling.
Reproducibility: Repo root `D:\work\Games\SRPG`; Godot 4.6.2 Steam executable available locally; execution date 2026-04-23.
Baseline: Before edits, the suite has 5 compilation failures, 17 assertion failures, and repeated runtime signal arity errors affecting attribute-related tests.
Execution: Gate verdict = PASS. Target, environment, side effects, and success signal are explicit. Challenge refinements applied: begin with P0 only, prefer shared-root-cause fixes, and use the full suite as the only completion gate.
