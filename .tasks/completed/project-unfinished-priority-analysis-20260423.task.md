Role: Senior technical lead delivering a repository-wide unfinished-work assessment for a Godot SRPG project.
Context: The repository has substantial design/production artifacts, implemented core systems, a playable prototype under `prototypes/vertical-slice`, a lightweight main-menu/battle scene path under `src/ui`, and a large GUT-style automated test suite. The user asked for a full-project analysis that identifies what remains unfinished and orders it by execution priority. Repository state must be grounded in code, tests, and active production artifacts rather than old reports alone.
Objective: Identify confirmed unfinished items across the entire project and rank them in the order the team should execute them next.
Success criteria: 1) Each unfinished item has direct evidence from source, tests, scenes, or production docs. 2) Priorities reflect dependency blocking and delivery risk. 3) The report distinguishes baseline defects, prototype-to-product gaps, unimplemented sprint scope, and later backlog epics. 4) Verification evidence is included for any claim about current test/build status.
Decomposition: 1. Inspect stage, sprint, and epic artifacts to establish intended delivery order. 2. Inspect source modules, scenes, and prototype files to identify implemented versus placeholder systems. 3. Run Godot parse/test verification to confirm current baseline quality. 4. Consolidate failures and missing implementations into root-cause buckets. 5. Deliver a priority-ordered unfinished-items report with rationale and residual risks.
Methodology: SCQA for the overall repository-state framing and MECE for decomposing unfinished work into baseline stability, productization blockers, sprint scope gaps, deferred epic backlog, and process/documentation debt.
Output: A concise Chinese markdown report listing unfinished items by priority, with supporting evidence and next-step ordering.
Constraints: Analysis only; do not modify runtime behavior. Avoid duplicating the same root cause across multiple items. Treat prototype-only functionality as incomplete until wired into the main product path. Treat manual status files as supporting evidence, not sole proof.
Non-assumptions: Do not assume passed subsystem tests imply complete user-facing integration. Do not assume `Status: Ready` means work started. Do not assume `production/sprints/sprint-001.md` is fully current when session-state contradicts it. Do not assume absence of a file means absence of a requirement unless a production epic/GDD demands it.
Verification: Use `Get-Content`/`rg` inspection for production docs and implementation coverage; run `godot.windows.opt.tools.64.exe --headless --check-only project.godot`; run `godot.windows.opt.tools.64.exe --headless res://tests/test_runner.tscn` and extract compile/assertion failures; verify main entry path via `project.godot` and related scenes/scripts.
Confidence: High on the top-priority items because they are supported by both repository artifacts and executable verification. Medium on lower-priority backlog sequencing because post-vertical-slice business priority may still change.
Reproducibility: Repo root `D:\work\Games\SRPG`; commands executed in PowerShell; Godot executable resolved from local installation; verification date 2026-04-23.
Baseline: Verified current state includes implemented core logic for attributes/class/resource/tactical/AI/turn-based/battle-settlement, placeholder presentation scenes under `src/ui`, a playable but isolated prototype under `prototypes/vertical-slice`, and automated test failures totaling 5 compilation failures plus 17 assertion failures.
Execution: Gate verdict = PASS. Target, environment, side effects, and verification signals are explicit. Challenge refinements applied: prioritize root-cause buckets over raw TODO counts, and separate prototype completion from shipping-path completion.
